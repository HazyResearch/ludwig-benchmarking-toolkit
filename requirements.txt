# Ludwig Mega-AutoML Benchmark dependencies
# Install with: pip install -r requirements.txt
# Ludwig itself should be installed separately (e.g. pip install -e /path/to/ludwig[full])

pyyaml>=6.0
rich>=13.0.0
pandas>=2.0.0
pyarrow>=14.0.0
scikit-learn>=1.3.0
openml>=0.14.0
duckdb>=0.10.0
filelock>=3.12.0
